apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo
  namespace: podinfo
spec:
  parentRefs:
    - name: shared-gateway
      namespace: istio-system
  hostnames:
    - podinfo.lon.pez.sh
  rules:
    - backendRefs:
        - name: podinfo
          port: 9898
