apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pez-sh
  namespace: pez-sh
spec:
  parentRefs:
    - name: shared-gateway
      namespace: istio-system
  hostnames:
    - pez-sh.lon.pez.sh
  rules:
    - backendRefs:
        - name: pez-sh
          port: 8080
