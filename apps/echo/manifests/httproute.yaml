apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: echo
  namespace: echo
spec:
  parentRefs:
    - name: shared-gateway
      namespace: istio-system
  hostnames:
    - echo.lon.pez.sh
  rules:
    - backendRefs:
        - name: echo
          port: 80
