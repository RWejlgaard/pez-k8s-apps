apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # App of apps: one ArgoCD Application per workload, each syncing its own
  # plain-manifest directory below.
  - apps/echo/application.yaml
  - apps/pez-sh/application.yaml
  - apps/podinfo/application.yaml
